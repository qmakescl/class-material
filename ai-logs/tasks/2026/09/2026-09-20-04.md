# 2026-09-20-04 작업 기록

- 시각: 22:54 KST
- AI: Claude Sonnet 5
- 상태: 완료

## 사용자 요청

> 이 대시보드에 시기별 타율을 평균과 비교하는 것을 추가하려면 어떤 방법이 좋겠니?
> (검토 후) 구현해줘

## AI 응답 요약

월별 타율을 막대 그래프로 보여주고 리그 시즌 평균을 기준선으로 겹쳐 표시하는 방식을
제안(고정 리그 평균 vs 현재 필터 평균 비교의 트레이드오프 설명)한 뒤, 승인을 받아
KBO 대시보드에 "월별 타율 vs 리그 시즌 평균" 차트를 구현했다. 박동원 월별 수치
(3월 .348, 4월 .306, 5월 .303, 8월 .177 등)가 직전 차시 개요 문서 §4 후보 A와
정확히 일치함을 확인했다.

## 수행 작업

- `app.py`: `/api/query`에 월별 집계(`strftime('%m', game_date)` 기준 `is_ab`/`is_hit`
  그룹핑)와 리그 시즌 전체 평균(`league_season_avg`, 필터 무관) 추가
- `templates/index.html`: "월별 타율 vs 리그 시즌 평균" 막대 차트 섹션 추가, 리그
  평균을 각 막대 트랙 위에 세로 기준선(`avg-marker`)으로 겹쳐 표시, 범례 추가
- 기존 타율 표시 로직을 `fmtAvg()` 헬퍼로 통일

## 변경 파일

- `src/python/kbo_dashboard/app.py`: 월별 집계·리그 시즌 평균 응답 필드 추가
- `src/python/kbo_dashboard/templates/index.html`: 월별 타율 차트 UI·JS 추가

## 검증

- `uv run python -m compileall -q src/python/kbo_dashboard/app.py` 통과
- 로컬 서버 기동 후 `GET /`에 `monthly-chart`, `league-avg-label` 요소 렌더링 확인
- `GET /api/query` → `league_season_avg` 0.262 확인
- `GET /api/query?batter=박동원` → 월별 타율 3월 .348 / 4월 .306 / 5월 .303 / 6월 .217
  / 7월 .234 / 8월 .177 / 9월 .258 — 직전 차시 개요 문서(§4 후보 A) 수치와 정확히 일치
- 서버 로그에 오류 없음 확인 후 프로세스 종료

## 결정 및 후속 작업

- 기준선은 "현재 필터가 걸린 리그 시즌 평균"이 아니라 "전체 무필터 리그 시즌 평균"으로
  고정 (팀/타자를 필터링해도 기준선은 리그 전체 기준을 유지해야 "이게 낮은 건가?" 질문에
  답할 수 있다는 설계 의도)
- 없음
