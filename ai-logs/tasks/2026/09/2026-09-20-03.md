# 2026-09-20-03 작업 기록

- 시각: 22:12 KST
- AI: Claude Sonnet 5
- 상태: 완료

## 사용자 요청

> 팀 이름을 한글로 다시 생성해주고, 주자 상황에 득점권을 추가해줘.
> 그리고 네가 이해하는 득점권은 무엇이지?

## AI 응답 요약

득점권의 정의(2·3루에 주자가 있는 상황, 1루만 있는 경우는 제외 — 직전 차시 개요
검토에서 쓰인 정의와 동일)를 설명한 뒤, KBO 대시보드의 팀 코드(HH/HT/KT/…)를
한글 구단명으로 매핑하고, 주자 상황 필터에 "득점권" 옵션(전체/득점권/득점권
아님)을 추가했다.

## 수행 작업

- `app.py`: `TEAM_LABELS` 딕셔너리(팀 코드 → 한글 구단명) 추가
- `app.py`: `build_filters()`에 `risp`(득점권) 조건 추가 — `on_2b IS NOT NULL OR
  on_3b IS NOT NULL` (yes) / 둘 다 NULL (no)
- `app.py`: 팀 드롭다운·표본 행의 팀 표시를 한글 라벨로 변경
- `templates/index.html`: 주자 상황 필드셋에 "득점권 (2·3루 주자)" 선택창 추가,
  팀 드롭다운을 (코드, 한글명) 튜플로 렌더링, JS에 `risp` 파라미터 반영

## 변경 파일

- `src/python/kbo_dashboard/app.py`: `TEAM_LABELS`, 득점권 필터, 한글 팀명 매핑 추가
- `src/python/kbo_dashboard/templates/index.html`: 득점권 선택창 추가, 팀 드롭다운 한글화

## 검증

- `uv run python -m compileall -q src/python/kbo_dashboard/app.py` 통과
- 로컬 서버 기동 후 팀 드롭다운이 한글 구단명(한화 이글스, KIA 타이거즈 등
  10개 구단)으로 렌더링됨을 확인
- `GET /api/query?risp=yes` → 타율 .271, `GET /api/query?risp=no` → 타율 .259로
  이전에 검증한 리그 득점권/비득점권 타율과 일치
- `GET /api/query?team=OB`의 표본 행 `batting_team`이 "두산 베어스"로 표시됨을 확인

## 결정 및 후속 작업

- 득점권 정의는 2·3루 기준으로 고정 (1루 포함 여부는 별도 필터로 선택 가능하므로
  득점권 정의 자체를 확장하지 않음)
- 없음
