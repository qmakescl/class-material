# 2026-09-14-04 작업 기록

- 시각: 17:21 KST
- AI: Codex
- 상태: 완료

## 사용자 요청

> 표준화계수를 보여주고 이를 클릭하며 X - 평균 설명에 이동 자체를 애니메이션으로 표현해주면 좋을 거 같다. 그리고 표준편차를 나누는 과정을 기존 정규분포의 퍼진 정도에서 표준정규분포의 퍼진 정도로 변하는 것도 당연히 추가하고

## AI 응답 요약

표준화 패널에 평균 이동과 표준편차 나눗셈을 각각 재생하는 버튼 및 애니메이션 캔버스를 추가했다. 두 번째 애니메이션은 분포 폭이 σ에서 1로 변하는 과정을 표시한다.

## 수행 작업

- `X − μ`와 `÷ σ` 단계별 재생 버튼을 추가했다.
- 평균을 뺄 때 곡선 중심과 관측점이 원점으로 이동하는 애니메이션을 구현했다.
- 표준편차로 나눌 때 곡선 폭과 관측값이 σ 척도에서 표준정규분포 척도로 압축되는 애니메이션을 구현했다.
- 각 단계에서 중심, 표준편차, 관측점의 변화를 캔버스에 설명으로 표시했다.
- 축소 모션 환경에서는 애니메이션 없이 최종 상태를 표시하도록 처리했다.

## 변경 파일

- `docs/statistics/grad-probability-lab.html`: 표준화 단계 버튼 및 애니메이션 렌더링 추가

## 검증

- 추출한 인라인 JavaScript에 `node --check /dev/stdin` 실행 — 성공
- `git diff --check` — 성공
- `UV_CACHE_DIR=/tmp/class-material-uv-cache uv run python -m compileall -q main.py` — 성공

## 결정 및 후속 작업

- 평균 이동 애니메이션은 큰 원래 평균값 때문에 곡선이 화면에서 점처럼 보이지 않도록, 평균 위치 관계를 강조하는 개념 축에서 표현했다.
