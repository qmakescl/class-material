# 2026-09-20-02 작업 기록

- 시각: 22:09 KST
- AI: Claude Sonnet 5
- 상태: 완료

## 사용자 요청

> 여기에 팀-타자를 추가해서 팀 또는 타자의 결과를 확인하도록 하자. 그리고 디자인 좀
> 개선해 주면 안될까? iOS 의 liquid glass 테마도 좋고 :)

## AI 응답 요약

KBO 2025 타석 탐색 대시보드(`src/python/kbo_dashboard/`)에 팀·타자 필터와 타율
통계 카드를 추가하고, 전체 UI를 iOS "liquid glass" 스타일(반투명 블러 패널,
그라디언트 배경, 라이트/다크 모드 대응)로 재구성했다. 로컬 서버로 팀/타자 필터,
타율 계산(노시환 .260 등 실측 확인) 동작을 검증했다.

## 수행 작업

- `ingest.py`: `batting_team` 파생 컬럼(공격팀, inning_topbot 기반) 추가, 관련
  인덱스(`batting_team`, `batter_name`) 추가, DB 재생성
- `app.py`: 팀/타자 필터 조건 추가, `/api/batters`(팀별 타자 목록) 엔드포인트
  신규 추가, 쿼리 응답에 타수·안타·타율(`batting_avg`) 통계 추가, 표본 행에
  `batting_team` 포함
- `templates/index.html`: 팀 드롭다운·타자 자동완성(datalist) 필터 UI 추가,
  전체 디자인을 liquid glass 테마로 재작성(블러 패널, 그라디언트, 라이트/다크
  모드 대응)
- README의 대시보드 설명 문구에 "팀·타자" 필터 언급 반영

## 변경 파일

- `src/python/kbo_dashboard/ingest.py`: `batting_team` 컬럼 및 인덱스 추가
- `src/python/kbo_dashboard/app.py`: 팀/타자 필터, `/api/batters`, 타율 통계 추가
- `src/python/kbo_dashboard/templates/index.html`: 필터 UI 확장 및 liquid glass 디자인 전면 개편
- `README.md`: 대시보드 설명 문구 갱신
- `datasets/kbo_dashboard.sqlite3`: 재생성 (커밋 대상 아님, `.gitignore` 처리됨)

## 검증

- `uv run python -m compileall -q src/python/kbo_dashboard/ingest.py src/python/kbo_dashboard/app.py` 통과
- `uv run python src/python/kbo_dashboard/ingest.py` 재실행: 타석 56,139건,
  리그 타율 .262 동일 유지 (스키마 확장만, 집계값 불변 확인)
- 로컬 서버 기동 후 `GET /`(200), 팀 드롭다운 렌더링, `GET /api/batters?team=HH`
  응답 확인
- `GET /api/query?team=HH&batter=권광민` → 18타석/18타수/타율 .167 확인
- `GET /api/query?batter=노시환` → 630타석/539타수/140안타/타율 .260 확인
  (직전 차시 개요 검토에서 확인한 실측 수치와 일치)

## 결정 및 후속 작업

- 팀 필터는 "타격 기준 팀"(공격 시점의 팀)으로 정의 — 수비 상황 필터는 이번
  범위에서 제외
- 디자인은 라이트/다크 모드 모두 지원하도록 CSS 변수로 분리
- 학생 각자 로컬 재현용 안내 문서는 여전히 후속 작업으로 남음
