# AGENTS.md

## 적용 범위

이 문서는 저장소 루트와 모든 하위 디렉터리에 적용한다. 더 구체적인
`AGENTS.md`가 하위 디렉터리에 있으면 해당 문서의 지침을 우선한다.

## 프로젝트 개요

- 프로젝트명: `class-material`
- 용도: 수업용 정적 HTML 자료를 GitHub Pages로 배포하는 프로젝트
- Python 버전: 3.12 이상 (`.python-version` 기준 3.12)
- 패키지 및 가상환경 관리자: `uv`
- 정적 사이트 원본: `docs/`
- 로컬 미리보기 진입점: `main.py`
- 배포: `.github/workflows/deploy-pages.yml`을 통한 GitHub Pages
- 사이트 디자인 가이드: `DESIGN.md` (색상 시스템, 내비게이션 패턴, 빈
  상태 패턴 등). `docs/` 내 HTML/CSS를 수정하기 전에 확인한다.

## 환경 및 의존성 관리

- 가상환경은 프로젝트 루트의 `.venv`를 사용한다.
- Python과 패키지 관련 명령은 직접 `python`/`pip`를 호출하지 말고 `uv`를
  통해 실행한다.
- 환경 동기화: `uv sync`
- 명령 실행: `uv run <command>`
- 런타임 의존성 추가: `uv add <package>`
- 개발 의존성 추가: `uv add --dev <package>`
- 의존성 제거: `uv remove <package>`
- `uv.lock`은 직접 편집하지 말고 `uv` 명령으로 갱신한다.
- Codex 샌드박스에서 기본 캐시 경로에 접근할 수 없으면 명령 앞에
  `UV_CACHE_DIR=/tmp/class-material-uv-cache`를 붙인다.

## 작업 원칙

- 요청과 관련된 파일만 수정하고 기존 사용자 변경 사항은 보존한다.
- 새 코드는 Python 3.12 문법과 표준 라이브러리를 우선 사용한다.
- 함수와 변수에는 의도가 드러나는 이름을 사용하고, 불필요한 추상화는
  피한다.
- 공개 함수나 이해하기 어려운 로직에는 짧은 설명 또는 docstring을
  추가한다.
- 비밀값, API 키, 개인별 환경 파일, `.venv`는 커밋하지 않는다.
- 새 실행 방법이나 사용자-facing 동작을 추가하면 `README.md`도 함께
  갱신한다.
- 사이트 내부 링크와 자산 경로는 GitHub Pages의 프로젝트 하위 경로에서도
  작동하도록 상대 경로를 사용한다.
- 배포 대상 파일은 `docs/` 안에 두고, 빌드 단계가 꼭 필요하지 않은 한
  순수 HTML/CSS/JavaScript 구성을 유지한다.

## 검증

- Python 변경 후 문법을 검사한다.

  ```sh
  uv run python -m compileall -q main.py
  ```

- 사이트 변경 후 로컬 서버를 실행해 HTML 문서와 주요 정적 자산이 정상
  응답하는지 확인한다.

- 테스트를 추가한 경우 전체 테스트 스위트를 실행한다. 현재 테스트 도구가
  정해져 있지 않으므로 도입이 필요하면 `pytest`를 개발 의존성으로 사용한다.
- 포매터나 린터가 프로젝트에 추가되면 해당 설정을 따르며, 설정되지 않은
  도구를 임의로 강제 적용하지 않는다.

## 완료 보고

- 파일 생성·변경 등 `AI_LOGGING.md`에서 정한 주요 작업을 마치면 최종 답변
  직전에 `ai-logs/YYYY/YYYY-MM-DD.md`에 요청, 응답 요약, 수행 작업 및 검증
  결과를 기록하고 일일 요약을 갱신한다.
- 파일 변경 없는 단순 질문·설명·검토는 기록하지 않는다. 세부 판단 기준과
  민감정보 처리에는 `AI_LOGGING.md`를 따른다.
- 수정한 내용과 검증 결과를 간결하게 요약한다.
- 실행하지 못한 검증이나 남은 위험이 있으면 명시한다.
