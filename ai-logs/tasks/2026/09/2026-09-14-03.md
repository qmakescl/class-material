# 2026-09-14-03 작업 기록

- 시각: 17:16 KST
- AI: Codex
- 상태: 완료

## 사용자 요청

> 잘 알려진 세가지 분포 다음에 표준화변화에 대한 설명을 넣어줘. 이떄 임의 정규분포로 부터 평균 만큼 이동하고, 표준편차로 나눠 표준정규분포가 되는 과정을 시각화를 통해 Z 변환계수를 설명하게 해줘

## AI 응답 요약

세 분포 실습 뒤에 정규분포 표준화 패널을 추가했다. 평균을 빼고 표준편차로 나누는 두 단계를 세 개의 분포 그림, 관측값 위치, Z값 및 누적확률 계산으로 연결했다.

## 수행 작업

- 표준화 수식과 Z값의 의미를 설명하는 패널을 추가했다.
- 평균, 표준편차, 관측값 슬라이더와 계산 결과 판을 추가했다.
- 원래 정규분포, 평균을 뺀 분포, 표준정규분포를 세로로 그려 축 단위가 바뀌는 과정을 시각화했다.
- 관측값이 평균에서 몇 표준편차 떨어졌는지와 `P(X ≤ x) = P(Z ≤ z)`를 함께 표시했다.

## 변경 파일

- `docs/statistics/grad-probability-lab.html`: 표준화 설명·컨트롤·캔버스 렌더링 로직 추가

## 검증

- 추출한 인라인 JavaScript에 `node --check /dev/stdin` 실행 — 성공
- `git diff --check` — 성공
- `UV_CACHE_DIR=/tmp/class-material-uv-cache uv run python -m compileall -q main.py` — 성공
- 로컬 브라우저 자동화 도구를 찾지 못해 실제 화면 상호작용 검증은 수행하지 못함

## 결정 및 후속 작업

- 관측값 슬라이더 범위를 현재 평균의 ±4σ로 갱신하여 모든 시각화에서 관측점이 그래프 범위 안에 있도록 했다.
