# 2026-09-14-06 작업 기록

- 시각: 17:28 KST
- AI: Codex
- 상태: 완료

## 사용자 요청

> 두 분포가 한 축에 있어야 변화를 잘 확인할 것 같다.

## AI 응답 요약

표준화 초기 비교 화면을 좌우 분할 그래프에서 하나의 공통 축 위에 겹쳐 그리는 방식으로 변경했다.

## 수행 작업

- 원래 정규분포와 표준정규분포를 같은 캔버스 축에 겹쳐 표시했다.
- 원래 평균 μ와 표준정규분포 평균 0을 같은 기준선에 표시했다.
- 원래 관측값과 대응 Z 관측값을 각각의 점선·점으로 표시했다.
- 원래 분포가 평균 0으로 이동하고 폭 1이 되면 표준정규분포와 겹친다는 설명을 추가했다.

## 변경 파일

- `docs/statistics/grad-probability-lab.html`: 표준화 초기 비교 그래프를 공통 축 오버레이로 변경

## 검증

- 추출한 인라인 JavaScript에 `node --check /dev/stdin` 실행 — 성공
- `git diff --check` — 성공
- `UV_CACHE_DIR=/tmp/class-material-uv-cache uv run python -m compileall -q main.py` — 성공

## 결정 및 후속 작업

- 평균과 단위가 다른 두 분포의 물리적 축을 그대로 합칠 수 없으므로, 중심·폭 변화 관계를 나타내는 공통 변환 축을 사용했다.
