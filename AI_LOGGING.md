# AI 대화 및 작업 기록 규칙

이 문서는 이 저장소에서 AI가 수행한 주요 변경 작업과 그 요청·응답을
일관되게 기록하기 위한 공통 규칙이다. Codex, Claude 등 저장소에서 작업하는
모든 AI에 동일하게 적용한다. 일반 대화 기록이 아니라 프로젝트 변경 이력을
보완하는 작업 일지를 만드는 것이 목적이다.

## 저장 위치와 파일명

- 기록 루트: `ai-logs/`
- 일자별 기록: `ai-logs/YYYY/YYYY-MM-DD.md`
- 기준 시간대: `Asia/Seoul` (KST)
- 같은 날짜의 대화는 하나의 파일에 시간순으로 이어 쓴다.
- `docs/`에는 기록을 저장하지 않는다. AI 기록은 GitHub Pages 배포 대상이
  아니다.
- 새 날짜 파일은 `ai-logs/templates/daily.md`를 복사해 시작한다.

## 기록 여부 판단

작업을 마친 시점의 저장소 상태를 기준으로 판단한다. 다음 중 하나에
해당하는 **주요 작업**만 기록한다.

- 프로젝트 파일을 새로 생성하거나 의미 있게 수정, 이동 또는 삭제한 작업
- 의존성, 실행 환경, 빌드, 배포, 테스트 또는 저장소 설정을 변경한 작업
- 여러 파일에 걸친 리팩터링, 콘텐츠 추가 또는 구조 개편
- 작업이 중단되었더라도 의미 있는 파일 변경이 저장소에 남은 경우
- 사용자가 특정 작업을 명시적으로 기록해 달라고 요청한 경우

다음 항목은 기록하지 않는다.

- 파일 변경 없이 답변만 제공한 단순 질문, 개념 설명 또는 추천
- 인사, 확인 응답, 상태 문의, 계획 논의 및 추가 정보 요청
- 읽기 전용 코드 검토나 조사처럼 저장소에 결과물이 남지 않은 작업
- 파일을 바꾸지 않은 실행·검증 명령과 실패한 시도
- `.pyc`, 캐시, 로그 등 임시·자동 생성 파일만 생긴 경우
- 오탈자 한 글자처럼 작업 이력으로서 가치가 낮은 사소한 변경. 단, 더 큰
  작업의 일부라면 해당 주요 작업 기록에 포함

판단이 애매하면 기록하지 않는 것을 기본으로 하며, 여러 작은 변경이 하나의
기능이나 목적을 이루면 하나의 주요 작업으로 묶어 기록한다.

각 요청 단위 기록에는 다음 내용을 포함한다.

1. `기록 ID`: `YYYY-MM-DD-NN` 형식의 날짜별 일련번호
2. `시각`: 요청을 처리한 KST 시각. 정확한 시각을 알 수 없으면 `미기록`
3. `AI`: 확인 가능한 도구 또는 에이전트 이름. 알 수 없으면 `AI`
4. `상태`: `완료`, `부분 완료`, `보류`, `중단` 중 하나
5. `사용자 요청`: 의미를 바꾸지 않은 원문. 길면 핵심 원문과 생략 표시
6. `AI 응답 요약`: 사용자에게 전달한 결론과 안내. 원문 전체 복사보다
   재현 가능한 충실한 요약을 우선
7. `수행 작업`: 실제로 수행한 조사, 명령, 구현 내용
8. `변경 파일`: 저장소 상대 경로와 변경 목적. 변경이 없으면 `없음`
9. `검증`: 실행한 검사와 결과. 실행하지 못했다면 그 이유
10. `결정 및 후속 작업`: 중요한 선택, 미해결 사항, 사용자가 해야 할 일

## 일일 요약

날짜 파일 상단의 `일일 요약`은 그날의 모든 기록을 반영해 계속 갱신한다.
다음 네 항목을 유지한다.

- 완료한 작업
- 주요 변경 파일
- 결정 사항
- 미해결 및 다음 작업

요약에는 대화 과정보다 결과를 적는다. 미해결 사항이 없으면 `없음`이라고
명시한다.

## 작성 시점과 변경 원칙

- AI는 기록 대상인 주요 작업을 마치고 최종 답변을 보내기 직전에 해당
  기록을 추가하고 일일 요약을 갱신한다. 기록 제외 대상에는 날짜 파일을
  만들거나 수정하지 않는다.
- 작업이 중단되거나 일부만 끝나도 상태와 실제 완료 범위를 기록한다.
- 기존 대화 항목은 원칙적으로 덮어쓰거나 삭제하지 않는다. 사실 오류를
  고칠 때는 원문을 보존하고 바로 아래에 `정정` 메모를 추가한다.
- 다른 AI가 만든 변경을 발견하면 보존하고, 마지막 번호 다음의 새 기록을
  추가한다.
- 동일 요청에 여러 AI가 참여하면 AI별 결과를 같은 기록 아래 구분하거나,
  독립적으로 사용자에게 응답했다면 별도 기록으로 남긴다.
- 추측을 사실처럼 기록하지 않는다. 확인하지 못한 내용은 `미확인`으로
  표시한다.

## 보안과 기록 제외 항목

다음 내용은 기록하지 않는다.

- API 키, 토큰, 비밀번호, 쿠키 및 기타 인증 정보
- 개인식별정보나 공개할 필요가 없는 로컬 환경 정보
- 시스템·개발자 지침 전문
- AI의 비공개 내부 추론 과정(chain-of-thought)
- 장황한 터미널 출력, 캐시 로그, 바이너리 내용

민감한 내용이 사용자 요청에 포함되어 맥락 보존이 필요하면 해당 부분을
`[REDACTED]`로 바꾼다. 오류 메시지와 명령 출력은 원문 전체 대신 원인과
결과만 요약한다.

## 표현 규칙

- 기록은 한국어로 작성하되 파일명, 코드, 명령은 원래 표기를 유지한다.
- 파일 경로는 저장소 기준 상대 경로로 쓰고 백틱으로 감싼다.
- 실행한 명령은 필요한 최소 범위만 백틱으로 기록한다.
- 응답 요약만 읽어도 사용자가 무엇을 요청했고 어떤 결과를 받았는지 알 수
  있어야 한다.
