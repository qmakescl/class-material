# 2026-09-20-01 작업 기록

- 시각: 22:03 KST
- AI: Claude Sonnet 5
- 상태: 완료

## 사용자 요청

> 자 그럼 이렇게 고민해보자. 먼저 학생들에게 보여질 dashboard 를 나의 로컬환경에 작동해서
> 함께 살펴보고, 데이터셋의 구조도 확인하게 한 후 이 데이터셋을 이용해 각자의 컴퓨터에서
> 작동하게 하는 것을 통해 외부데이터셋을 이용해서 각자의 로컬에서 분석을 통해 이야기하고자
> 하는 것의 수치와 그래프를 생성하게 하도록 하는 것은 어떨까? (이후 "스토브리그" 차시 개요
> 첨부·검토 요청과, "이 데이터셋은 공개되어 있지만, 철저히 원저작자를 밝히고, 첨부한
> 차시개요의 2장 데이터 섹션의 주자상황, events, 해당 타석 득점을 선택해서 볼 수 있는
> 대시보드를 만든다. 데이터를 공개하지 않는다. 백엔드는 sqlite3 정도로 단순하게"라는
> 후속 요청으로 이어짐.

## AI 응답 요약

첨부된 차시 개요(스토브리그 PT 재현)를 검토해 시간 배분 합계 오류, 핵심 수치(리그 타율
.262/.271 등) 미검증, 카드 배포 기준 모호함, 데이터 배치 컨벤션 누락 등을 피드백했다.
이후 사용자가 방향을 "로컬 전용 KBO 2025시즌 타석 탐색 대시보드(sqlite3 백엔드, Flask,
데이터 비공개)"로 확정하여 실제 구현했다. 데이터 실측 결과 리그 전체 타율 .262, 득점권
타율 .271, 타석 수 56,139로 차시 개요 문서의 §3.1 수치와 정확히 일치함을 확인했다.

## 수행 작업

- `uv add pyarrow flask`로 의존성 추가
- Hugging Face `slothman3878/kbo_playbyplay` 2025시즌 parquet(투구 단위)을 내려받아
  타석(PA) 단위로 축약하고 `is_ab`/`is_hit`를 부여하는 `ingest.py` 작성 및 실행
- `datasets/kbo_dashboard.sqlite3` 생성 (plate_appearances, dataset_meta 테이블)
- 주자 상황(on_1b/2b/3b)·타석 결과(events)·타석당 득점(runs_scored)을 필터링해
  보여주는 Flask 대시보드(`app.py`, `templates/index.html`) 작성
- 로컬 서버 기동 후 `/`, `/api/query` 엔드포인트 스모크 테스트
- README에 실행 방법 추가, `.gitignore`에 원본/가공 데이터 파일 제외 규칙 추가

## 변경 파일

- `src/python/kbo_dashboard/ingest.py`: HF parquet → sqlite3 변환 스크립트 신규 작성
- `src/python/kbo_dashboard/app.py`: Flask 대시보드 서버 신규 작성
- `src/python/kbo_dashboard/templates/index.html`: 대시보드 UI 신규 작성
- `README.md`: 로컬 전용 대시보드 실행 안내 절 추가
- `.gitignore`: `datasets/kbo_pbp_*.parquet`, `datasets/kbo_dashboard.sqlite3` 제외 추가
- `pyproject.toml`, `uv.lock`: `pyarrow`, `flask` 의존성 추가 (uv add로 갱신)

## 검증

- `uv run python -m compileall -q src/python/kbo_dashboard/ingest.py src/python/kbo_dashboard/app.py` 통과
- `uv run python src/python/kbo_dashboard/ingest.py` 실행: 타석 56,139건, 리그 타율 .262,
  득점권 타율 .271 산출 (차시 개요 문서 수치와 일치)
- 로컬 Flask 서버 기동 후 `curl`로 `GET /` (200), `GET /api/query?on_2b=yes&on_3b=any` 응답
  확인, HTML `<title>` 및 한글 렌더링 확인

## 결정 및 후속 작업

- 원본/가공 데이터 파일은 리포지토리에 커밋하지 않음 (재배포 방지, `.gitignore` 처리)
- 학생 각자의 로컬 실행(각자 컴퓨터에서 ingest+대시보드 재현)은 다음 단계 작업으로 남음
- 차시 개요 문서의 시간 배분 합계 오류는 사용자 요청에 따라 이번 작업 범위에서 제외
