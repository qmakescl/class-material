# 2026-09-14-01 작업 기록

- 시각: 17:03 KST
- AI: Codex
- 상태: 완료

## 사용자 요청

> `docs/statistics/grad-probability-lab.html`에서 보여주는 도표들이 초기값으로는 잘 그려지는데 설정값을 바꾸고 나면 화면을 벗어나거나 제대로 그려지지 않는다. 축 변화도 필요하니 제대로 렌더링되게 수정해줘

## AI 응답 요약

설정 변경 뒤 캔버스 좌표계가 고해상도 화면에서 반복 확대되던 문제를 수정하고, 정규분포와 상대위험 도표의 범위 처리를 보완했다.

## 수행 작업

- 캔버스의 CSS 논리 크기를 별도로 보존하여 재렌더링마다 내부 해상도가 누적 확대되지 않도록 수정했다.
- 정규분포의 가로축을 평균과 표준편차에 따라 다시 계산하고, 눈금 간격도 범위에 맞게 조정했다.
- 상대위험 계산 결과가 100%를 넘는 경우 도표를 확률 범위 안에서 표시하고 한계를 안내하도록 처리했다.

## 변경 파일

- `docs/statistics/grad-probability-lab.html`: 캔버스 렌더링과 축 범위 처리 수정

## 검증

- `UV_CACHE_DIR=/tmp/class-material-uv-cache uv run python -m compileall -q main.py` — 성공
- `git diff --check` — 성공
- 로컬 미리보기 서버에서 `curl -I http://127.0.0.1:8123/statistics/grad-probability-lab.html` — HTTP 200 확인
- 브라우저 자동화 환경은 사용할 수 없어 실제 UI 상호작용 검증은 수행하지 못함

## 결정 및 후속 작업

- 설정값 변경 시 모든 도표가 공통 캔버스 초기화 함수를 사용하므로, 근본 원인을 공통 함수에서 해결했다.
