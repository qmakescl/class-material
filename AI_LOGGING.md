# AI 주요 작업 기록 규칙

이 문서는 AI가 수행한 주요 파일 변경 작업을 독립적인 상세 기록과 날짜별
요약으로 관리하기 위한 공통 규칙이다. Codex, Claude 등 저장소에서 작업하는
모든 AI에 동일하게 적용한다.

## 디렉터리 구조

```text
ai-logs/
├── tasks/YYYY/MM/YYYY-MM-DD-NN.md
├── daily/YYYY/MM/YYYY-MM-DD.md
└── templates/
    ├── task.md
    └── daily.md
```

- 기준 시간대는 `Asia/Seoul`(KST)이다.
- `NN`은 날짜마다 `01`부터 시작하는 두 자리 일련번호다.
- 상세 기록의 파일명이 곧 기록 ID다. 예:
  `ai-logs/tasks/2026/09/2026-09-12-04.md`
- 일일 요약은 같은 날짜의 상세 기록을 한 행씩 연결한다.
- `docs/`에는 기록을 저장하지 않는다. AI 기록은 GitHub Pages 배포 대상이
  아니다.

## 기록 여부 판단

다음 중 하나에 해당하는 주요 작업만 기록한다.

- 프로젝트 파일을 새로 생성하거나 의미 있게 수정, 이동 또는 삭제한 작업
- 의존성, 실행 환경, 빌드, 배포, 테스트 또는 저장소 설정을 변경한 작업
- 여러 파일에 걸친 리팩터링, 콘텐츠 추가 또는 구조 개편
- 작업이 중단되었더라도 의미 있는 파일 변경이 저장소에 남은 경우
- 사용자가 특정 작업을 명시적으로 기록해 달라고 요청한 경우

다음 항목은 기록하지 않는다.

- 파일 변경 없이 답변만 제공한 단순 질문, 개념 설명 또는 추천
- 인사, 확인 응답, 상태 문의, 계획 논의 및 추가 정보 요청
- 읽기 전용 코드 검토나 조사처럼 저장소에 결과물이 남지 않은 작업
- 파일을 바꾸지 않은 실행·검증 명령과 실패한 시도
- `.pyc`, 캐시, 로그 등 임시·자동 생성 파일만 생긴 경우
- 작업 이력으로서 가치가 낮은 사소한 변경. 단, 더 큰 작업의 일부라면 해당
  주요 작업 기록에 포함한다.

판단이 애매하면 기록하지 않는다. 같은 목적의 여러 변경은 하나의 작업
기록으로 묶는다.

## 상세 작업 기록

주요 작업 하나마다 `ai-logs/templates/task.md`를 바탕으로 별도 파일을 만든다.
상세 기록에는 다음 항목을 모두 포함한다.

1. 기록 ID, 시각, AI 이름, 상태
2. 의미를 바꾸지 않은 사용자 요청 원문
3. 사용자에게 전달한 AI 응답 요약
4. 실제 수행 작업
5. 변경 파일과 변경 목적
6. 실행한 검증과 결과
7. 결정 사항 및 후속 작업

상태는 `완료`, `부분 완료`, `보류`, `중단` 중 하나를 사용한다. 정확한 시각이나
AI 이름을 확인할 수 없으면 `미기록` 또는 `AI`로 쓴다.

## 일련번호 부여

- 작업 시작 시 해당 날짜의 `tasks/YYYY/MM/` 파일을 확인한다.
- 가장 큰 `NN`의 다음 번호를 사용한다. 기록이 없으면 `01`을 사용한다.
- 기존 번호는 파일을 이동하거나 정정해도 다시 사용하지 않는다.
- 다른 AI가 같은 번호를 먼저 사용했다면 기존 파일을 보존하고 다음 빈
  번호로 변경한다.

## 날짜별 요약

`ai-logs/daily/YYYY/MM/YYYY-MM-DD.md`에는 그날의 주요 작업을 Markdown 표로
요약한다. 표의 열은 다음 순서를 유지한다.

| ID | 시각 | AI | 작업 요약 | 주요 변경 파일 | 상태 |
|---|---|---|---|---|---|

- ID는 해당 상세 작업 파일로 연결되는 상대 링크로 작성한다.
- 작업 하나당 한 행만 사용하며, 상세 과정은 반복하지 않는다.
- 주요 변경 파일은 대표 경로만 `<br>`로 구분해 간결하게 적는다.
- 상세 기록을 추가하거나 상태를 정정할 때 같은 날짜의 요약 표도 즉시
  갱신한다.
- 기록 대상 작업이 없는 날짜에는 일일 파일을 만들지 않는다.

## 작성 시점과 보존 원칙

- 주요 작업을 마치고 최종 답변을 보내기 직전에 상세 작업 파일을 만들고
  일일 요약 표에 한 행을 추가한다.
- 작업이 중단되거나 일부만 끝나도 의미 있는 변경이 남았다면 실제 완료
  범위와 상태를 기록한다.
- 기존 상세 기록은 덮어쓰거나 삭제하지 않는다. 사실 오류는 파일 하단에
  `정정` 절을 추가하고 일일 요약의 해당 행도 최신 상태로 고친다.
- 추측을 사실처럼 기록하지 않으며 확인하지 못한 내용은 `미확인`으로 쓴다.

## 보안과 기록 제외 항목

다음 내용은 기록하지 않는다.

- API 키, 토큰, 비밀번호, 쿠키 및 기타 인증 정보
- 개인식별정보나 공개할 필요가 없는 로컬 환경 정보
- 시스템·개발자 지침 전문
- AI의 비공개 내부 추론 과정(chain-of-thought)
- 장황한 터미널 출력, 캐시 로그, 바이너리 내용

민감한 내용이 요청에 포함되어 맥락 보존이 필요하면 `[REDACTED]`로 바꾼다.
오류 메시지와 명령 출력은 원문 전체 대신 원인과 결과만 요약한다.

## 표현 규칙

- 기록은 한국어로 작성하되 파일명, 코드, 명령은 원래 표기를 유지한다.
- 파일 경로는 저장소 기준 상대 경로로 쓰고 백틱으로 감싼다.
- 실행 명령은 필요한 최소 범위만 기록한다.
- 상세 기록만 읽어도 요청, 결과, 변경 및 검증 내용을 재구성할 수 있어야
  한다.
